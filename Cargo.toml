[package]
name = "nevercore"
version = "0.1.0"
edition = "2024"

[dependencies]
slint = "1.3"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rfd = "0.14" # Для диалогов открытия/сохранения

[build-dependencies]
slint-build = "1.3"